/////Task: This challenge requires you to print on a single line,
and then print the already provided input string to stdout. If you are not familiar with C, you may want to read about the printf() command.////

#include <stdio.h>

int main() 
{
    printf("Hello, World!\n");
    char s[] = "Welcome to C programming.";
    printf("%s\n", s);
    return 0;
}
